language: php

php:
  - '7.0'
  - '7.1'
  - nightly

install:
  - composer install --prefer-source

script:
  - mkdir -p build/logs
  - composer coverage
  - composer lint

after_success:
  - composer cover
